<mvc:View
controllerName="sap.ui.demo.walkthrough.controller.InvoiceList"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc">
   <List
      id="invoiceList"
      headerText="{i18n>invoiceListTitle}"
      class="sapUiResponsiveMargin"
      width="auto"
       
      items="{
         path : 'invoice>/Invoices',
         sorter : {
            path : 'ShipperName',
				group : true
         }
      }">
      <headerToolbar>
         <Toolbar>
            <Title text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer/>
            <SearchField width="50%" search=".onFilterInvoices"/>
         </Toolbar>
      </headerToolbar>
      <items>
         <ObjectListItem
            icon="{path: 'invoice>Icon'}"
            title="{invoice>Quality}{invoice>Quantity} x {invoice>ProductName} {invoice>ExtendedPrice}"
            number="{
               path: 'invoice>ExtendedPrice',
               type: 'sap.ui.model.type.Currency',
               formatOptions: {
                  showMeasure: true
               }
            }"
            numberState="{= ${invoice>ExtendedPrice/0} > 50 ? 'Error' : 'Success' }"
            intro="{
               parts:[{path:'invoice>ExtendedPrice/0'},{path:'invoice>Quantity'},{path:'invoice>ExtendedPrice/1'}],
                    formatter: '.formatter.fullPrice'
            }"
            >
				<firstStatus>
					<ObjectStatus text="{
						path: 'invoice>Status',
						formatter: '.formatter.statusText'
					}"/>
				</firstStatus>
			</ObjectListItem>
      </items>
   </List>
</mvc:View>